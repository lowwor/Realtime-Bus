<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    xmlns:app="http://schemas.android.com/tools">
<data>
    <variable name="viewModel" type="com.lowwor.realtimebus.viewmodel.BusStationViewModel" />
</data>
<android.support.v7.widget.CardView
    android:id="@+id/card_view"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardUseCompatPadding="true"
    android:layout_gravity="center_horizontal"
    app:cardBackgroundColor="@android:color/white"
    android:layout_marginTop="8dp"
    android:layout_marginLeft="12dp"
    android:layout_marginRight="12dp">
<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="16dp"
    android:layout_marginBottom="16dp"
    android:layout_marginLeft="20dp"
    android:layout_marginRight="20dp"
    >
    <ImageView
        android:id="@+id/alarm"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:layout_marginRight="16dp"
        android:scaleType="fitCenter"
        android:src="@{viewModel.isAlarm?@drawable/ic_alarm_on:@drawable/ic_alarm_add}"
        android:alpha="@{viewModel.isAlarm?100:30}"
        android:onClick="@{viewModel.onClickAlarm}"
        />

    <TextView
        android:id="@+id/tv_station_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/alarm"
        android:layout_centerVertical="true"
        android:text="@{viewModel.busStationName}"
        />

    <TextView
        android:id="@+id/tv_bus_number"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_marginRight="16dp"
        android:textColor="@color/primary"
        android:layout_centerVertical="true"
        android:text="@{viewModel.busNumber !=0 ?String.valueOf(viewModel.busNumber):null}"

        />
</RelativeLayout>

</android.support.v7.widget.CardView>



</layout>